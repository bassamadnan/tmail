package tui

// This file can be used for reusable tview components if your UI grows complex.
// For now, it can be empty or contain comments.

// Example (not used yet):
/*
import (
	"github.com/rivo/tview"
	"github.com/gdamore/tcell/v2"
)

// NewStyledTextView creates a text view with default styling.
func NewStyledTextView(text string) *tview.TextView {
	tv := tview.NewTextView().
		SetText(text).
		SetTextColor(tcell.ColorWhite).
		SetBackgroundColor(tcell.ColorDefault)
	return tv
}
*/
